name: notification_all
description: A simple command-line application.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: ">=2.15.1 <3.0.0"

# dependencies:
#   path: ^1.8.0

dev_dependencies:
  ffigen: ^4.1.2
  lints: ^1.0.0
dependencies:
  ffi: ^1.1.2

ffigen:
  output: "notify_go.dart"
  headers:
    entry-points:
      - "notification_go/notify.h"
